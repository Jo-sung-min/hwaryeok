# Next.js 서버에서 Spring Boot API를 호출할 때 사용합니다.
API_URL=http://localhost:8080/api/v1

# 브라우저에서 직접 호출하는 기능을 추가할 때 사용합니다.
# 공개되어도 되는 API 주소만 NEXT_PUBLIC_ 변수에 설정하세요.
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
