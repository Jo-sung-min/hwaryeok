spring:
  datasource:
    url: jdbc:h2:mem:hwaryeok-local;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
    hikari:
      schema: public
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: public
  flyway:
    enabled: true
    create-schemas: true
    default-schema: public
    schemas: public

server:
  port: ${SERVER_PORT:8081}
  servlet:
    session:
      cookie:
        secure: false

management:
  endpoint:
    health:
      show-details: always

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3001}
  oauth:
    frontend-base-url: ${OAUTH_FRONTEND_BASE_URL:http://localhost:3001}
  auth:
    jwt-secret: local-development-secret-change-before-production-2026
    license-hash-secret: local-license-hash-secret-change-before-production-2026
